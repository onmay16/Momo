import { Text, View, StyleSheet } from 'react-native';
import React from 'react';

const ClosetScreen = () => {
    return (
        <View>
            <Text>ClosetScreen</Text>
        </View>
    );
};

export default ClosetScreen;

const styles = StyleSheet.create({});
