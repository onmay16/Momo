import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

import { Category } from '../CustomComponent/Category';

export const CategoryRoutineList = () => {
  return (
    <View>
      <Category name="건강"/>
    </View>
  );
};

const styles = StyleSheet.create({});
